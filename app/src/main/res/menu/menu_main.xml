<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity" >
    <item
        android:id="@+id/action_search"
        android:orderInCategory="80"
        android:title="action_search"
        android:icon="@mipmap/ic_action_search"
        app:showAsAction="ifRoom"/>

    <!--<item
        android:id="@+id/action_scan_local_book"
        android:title="@string/menu_main_scan_local_book"
        android:icon="@mipmap/home_menu_scan"
        app:showAsAction="never" />-->

    <item
        android:id="@+id/action_night_mode"
        android:title="@string/menu_main_night_mode"
        android:icon="@mipmap/theme_night"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_settings"
        android:title="@string/settings"
        android:icon="@mipmap/home_menu_setting"
        app:showAsAction="never" />
</menu>